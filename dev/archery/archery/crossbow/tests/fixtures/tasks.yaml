groups:
  test:
    - test-*

  nightly:
    - test-*
    - nightly-*

  nightly-blocklist:
    - nightly-not-fine
    - test-a-test-two


tasks:
  test-a-test:
    ci: azure
    template: azure-linux.yaml

  test-a-test-two:
    ci: azure
    template: azure-linux.yaml

  nightly-fine:
    ci: azure
    template: azure-linux.yaml

  nightly-not-fine:
    ci: azure
    template: azure-linux.yaml
