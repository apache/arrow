// TypeScript configuration for the source target's tests
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "target": "esnext",
    "module": "commonjs",
    "paths": {
      "apache-arrow": [
        "src/Arrow.node"
      ],
      "apache-arrow/*": [
        "src/*"
      ]
    }
  }
}
